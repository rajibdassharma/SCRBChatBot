# Core backend
fastapi
uvicorn[standard]
python-dotenv
python-multipart
requests
pymysql
gunicorn

# Auth
pyjwt
passlib==1.7.4
bcrypt==4.0.1

# Document parsing
pypdf
python-docx
openpyxl

# ML / Embeddings
# NOTE: Install torch FIRST, then the rest.
# For GPU: pip install torch --index-url https://download.pytorch.org/whl/cu124
# For CPU (compatible with docling): pip install torch
torch
sentence-transformers

# Vector DB & search
chromadb
rank-bm25

# SMAC document parsing (Docling + OCR)
docling
easyocr

# Misc
pyspellchecker
