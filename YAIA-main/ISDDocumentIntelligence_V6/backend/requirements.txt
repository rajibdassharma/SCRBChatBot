# Core backend
fastapi
uvicorn[standard]
python-dotenv
python-multipart
requests
pymysql
gunicorn

# Auth
pyjwt
passlib==1.7.4
bcrypt==4.0.1

# Document parsing
pypdf
python-docx
openpyxl

# ML / Embeddings
# NOTE: Install torch + torchvision + torchaudio FIRST (matched versions, same index), then the rest.
#   For GPU (CUDA 12.4):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   For CPU only:
#     pip install torch torchvision torchaudio
# torchvision is REQUIRED — docling-ibm-models depends on it. Without it,
# the docling import in llm_kv_extractor.py / ocr_index_smac.py fails.
torch
torchvision
torchaudio
sentence-transformers

# Vector DB & search
chromadb
rank-bm25

# SMAC document parsing (Docling + OCR)
docling
easyocr

# Misc
pyspellchecker
